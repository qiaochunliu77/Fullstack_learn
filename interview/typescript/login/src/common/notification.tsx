import * as React from 'react'

interface Props {
    message:string;
    show: boolean;
    obClose:() => void;
}
export const NotificationComponent: React.FC<Props> = (props) => {
    return (
        
    )
}